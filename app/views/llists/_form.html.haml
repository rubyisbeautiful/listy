= semantic_form_for(@llist) do |f|
  - if @llist.errors.any?
    #error_explanation
      %h2
        = pluralize(@llist.errors.count, "error")
        prohibited this listy_list from being saved:
      %ul
        - @llist.errors.full_messages.each do |msg|
          %li= msg
  = f.inputs
  .actions
    = f.actions
